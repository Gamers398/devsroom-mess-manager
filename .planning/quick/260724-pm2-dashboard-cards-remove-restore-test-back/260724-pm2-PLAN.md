# Quick Task 260724-pm2 — Dashboard cards, remove restore-test, backup visibility, README guide

**Mode:** quick (default — no validate/discuss/research flags)
**Created:** 2026-07-24
**Branch:** master (direct — worktrees disabled for this run; change is interconnected and the operator works on master)

## Goal

Ship four confirmed items from the 2026-07-24 batch:
1. Dashboard: add a "Total meals (this month)" card and split the single net-balance card into **Total Credit** + **Total Dues**.
2. Remove the **restore-test** subsystem entirely (nightly scratch-DB job, button, service, model, table, config, connection). Keep real Backups + one-click Restore.
3. Make the backup system's automatic behaviour visible: log scheduled `backup:run`/`backup:purge` outcomes to the on-page Activity log, and add a **scheduler-health banner** that surfaces a missing server cron.
4. Rewrite **README.md** as a full per-feature user guide (includes the balance-system explainer + the cron/backup section).

Item #3 of the batch (why member balances exist) and the #6 diagnosis (why no backups were made) were answered in prose to the user — not code tasks.

## Locked decisions (from AskUserQuestion)

- D1 Dashboard cards = replace the net card with **Total Dues + Total Credit** (two cards), plus add **Total meals (this month)**.
- D2 "Test system" = the **restore-test** subsystem only (NOT PHPUnit tests, NOT demo seed data).
- D3 Backup code work = **scheduler-health banner** + **log scheduled runs on-page** (the "no code" option was also selected but is superseded by these two explicit code asks; the written diagnosis is already delivered).
- D4 Docs = rewrite **README.md** as the full feature guide.

## Workstream 1 — Dashboard cards

**files:**
- `app/Services/DashboardService.php` — `managerCards()`: add `total_meals` (from `$preview['total_meals']`), `total_credit` (Σ `advance_balance`), `total_dues` (Σ `due_balance`). Keep `total_member_balance` key for backward compat.
- `resources/views/home.blade.php` — add a "Total meals (this month)" `<x-stat-card>`; replace the "Member balances (net)" card with two cards: "Total credit (advance)" and "Total dues". Update the `$cards` default array.
- `tests/Feature/Dashboard/ManagerDashboardTest.php` — assert the new card labels/values.

**action:** Bill-derived totals already come from the cached `$preview` — no new queries. Money via `Money::taka()`. Meals via `number_format(..., 1)`.
**verify:** `php -d memory_limit=512M vendor/bin/phpunit --filter ManagerDashboard` green; `/home` renders 7 cards.
**done:** 3 new card keys in `managerCards()`; view renders Total meals + Total credit + Total dues; tests updated.

## Workstream 2 — Remove restore-test

**delete:**
- `app/Services/RestoreTestService.php`
- `app/Console/Commands/RestoreTestRun.php`
- `app/Models/RestoreTest.php`
- `database/factories/RestoreTestFactory.php`
- `tests/Feature/Backup/RestoreTestServiceTest.php`
- `resources/views/dashboard/backups/_health_badge.blade.php` (orphaned; references RestoreTest)

**add:**
- `database/migrations/2026_07_24_120000_drop_restore_tests_table.php` — forward-only `dropIfExists('restore_tests')`. (Not user data; safe.)

**edit:**
- `routes/console.php` — remove the `backup:restore-test` schedule block + `restore_test_enabled` guard.
- `routes/web.php` — remove the `restore-test.run` POST route (line 83).
- `app/Http/Controllers/Backup/BackupController.php` — remove `runRestoreTest()` + the `restoreTestEnabled` view var in `indexData()`.
- `resources/views/dashboard/backups/index.blade.php` — remove the "Run restore-test" button block (lines 16-23).
- `config/backup.php` — remove the `restore_test_enabled` key block.
- `config/database.php` — remove the `mysql_restore_test` connection block.
- `.env.example` — remove `DB_RESTORE_TEST_DATABASE` + `BACKUP_RESTORE_TEST_ENABLED` lines.
- `app/Models/BackupLog.php` — fix doc comment (drop restore-test reference).
- Tests that reference restore-test route/schedule: `BackupControllerAuthTest.php`, `BackupRunNowLoggingTest.php`, `ScheduledBackupCommandsTest.php`, `SpatieFailureNotificationListenerTest.php` — remove/adjust restore-test assertions.

**verify:** `php -d memory_limit=1024M vendor/bin/phpunit` green; no class reference to `RestoreTest*` or `backup:restore-test` remains (`grep -r`).
**done:** subsystem fully removed; drop migration present; tests green.

## Workstream 3 — Backup visibility

**add:**
- `app/Listeners/LogScheduledBackupActivity.php` — listens to spatie `BackupWasSuccessful`, `BackupHasFailed`, `CleanupWasSuccessful`, `CleanupHasFailed`, `UnhealthyBackupWasFound`, `HealthyBackupWasFound`. Writes a `BackupLog` row **only when `app()->runningInConsole()`** (scheduled/CLI runs — manual HTTP "Backup now" is already logged by the controller, so no double-log). Maps events → action/status (`backup`/`purge`/`monitor` × success/failure).

**edit:**
- `app/Providers/AppServiceProvider.php` — register the new listener for the six events inside `registerBackupFailureListeners()` (class_exists-guarded).
- `app/Http/Controllers/Backup/BackupController.php` — `indexData()`: compute `schedulerHealthy` (bool) + `schedulerIssue` (string) from `BackupConfig::current()->frequency` vs the newest backup timestamp (max of newest zip `last_modified` and newest success `BackupLog`). Cadence thresholds: off→always healthy; daily→warn >25h; weekly→warn >7.5d; monthly→warn >31d. Pass to view.
- `resources/views/dashboard/backups/index.blade.php` — render a warning banner at the top when `!$schedulerHealthy`, showing the exact cron line to install.

**verify:** `phpunit --filter Backup` green; new test asserting the listener logs a scheduled success + skips when not runningInConsole; controller computes healthy/unhealthy correctly.
**done:** scheduled backup outcomes appear in the Activity log; missing-cron banner shows when cadence is missed.

## Workstream 4 — README full feature guide

**files:** `README.md` (rewrite).
**action:** Restructure into: overview → install/quickstart (Laravel 13 + MySQL 8, `.env`, `migrate`, cron line, storage:link) → roles (super-admin/admin/member) → per-feature how-to: Members, Meal grid, Meal-off, Bazar, Expenses, Payments + Advance/Wallet, **Balance system explainer** (credit vs dues vs net, why two columns), Month-close, Reports + exports, Dashboard cards, Backups (schedule, "Backup now", Download/Restore/Delete, cron requirement, off-site destination). Bengali-ready note.
**verify:** links/commands accurate vs current code; `php artisan route:list` names match.
**done:** single comprehensive README.

## Execution

Atomic commit per workstream (code only). Final docs commit (PLAN + SUMMARY + STATE) at the end. Run targeted PHPUnit per workstream; full suite at the end (`php -d memory_limit=1024M vendor/bin/phpunit`). Pint (`vendor/bin/pint --test`) clean before each commit.
